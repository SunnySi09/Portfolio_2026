# Project Scoped Rules for Sunny Portfolio

- **Local Development Focus**: All coding changes, updates, or testing must be done strictly on the local workspace/server first.
- **Git Push Constraint**: Do not stage, commit, or run `git push` command automatically. Pushing to the remote GitHub repository (`Portfolio_2026`) must only be triggered when the user explicitly requests/approves a git push action.
